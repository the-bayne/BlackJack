<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baynecorp.blackjack">

    <application
        android:name=".BlackJackApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.BettingActivity"
            android:label="Bets"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".ui.MainActivity">
        </activity>
        <activity android:name=".ui.StatisticsActivity"
            android:label="Statistics"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".ui.MainActivity">
        </activity>
        <activity android:name=".ui.GameScreen"
            android:label="Statistics"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".ui.BettingActivity">
        </activity>
    </application>

</manifest>